name: CI

on:
  push:
    branches:
      - modern-ai-analysis-workbench
      - master
  pull_request:

jobs:
  analysis-service:
    name: Analysis service tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install -r analysis_service/requirements.txt

      - name: Run tests
        env:
          PYTHONPATH: ${{ github.workspace }}/analysis_service
        run: python -m pytest -q analysis_service/tests

  docker-build:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build analysis service image
        run: docker compose build
